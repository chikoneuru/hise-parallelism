global:
  scrape_interval: 15s

scrape_configs:
  - job_name: hise-orchestrator
    static_configs:
      - targets: ["orchestrator:8000"]
    metrics_path: /metrics

  - job_name: hise-workers
    dns_sd_configs:
      - names: ["worker-1", "worker-2"]
        type: A
        port: 9001
